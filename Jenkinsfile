pipeline {
    agent any

    stages {
        stage(Build) {
            steps {
                sh ls -la
                sh pwd
                sh whoami
                echo "Hello world"	
            }
        }
    }
}
